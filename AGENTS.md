# AGENTS.md

## 项目定位

本仓库只服务《正在发生的世界史》这一长期连载项目。核心目标是长期、稳定地发布每日一回的中文长文，并保持历史内容可追溯。

## 内容规则

1. 默认每天发布一回，一回对应一个 `docs/_posts/YYYY-MM-DD-NNN.md` 文件。
2. 回目采用“第X回 + 标题”的章回体格式；正文开头保留一段“定场诗”。
3. 文章可以有文学化叙事和转场，但人物、国家、机构、日期、数字、事件关系等事实信息必须有现实依据；不确定的信息不要补写成确定事实。
4. 正文以普通读者能连续读下去为第一优先级，避免新闻简报式堆点，也避免学术论文式表达。
5. 已发布文章原则上不做无痕重写。事实需要纠正时，在文章末尾增加“更正记录”，写明修改日期和内容。
6. 不把评论区传闻、未经确认的社交媒体消息写成既成事实。

## 文件约定

每篇文章 Front Matter 至少包含：

```yaml
---
layout: post
title: "第一回 ……"
episode: 1
date: 2026-09-15 21:00:00 +0800
summary: "一句话概括"
permalink: /episodes/001/
---
```

正文建议结构：

```markdown
> 定场诗四句。

开场正文……

<!--more-->

后续正文……
```

`<!--more-->` 之前的内容会作为首页摘要的一部分。

## 站点规则

- GitHub Pages 来源固定为 `main` 分支 `/docs` 目录。
- 站点保持纯 Jekyll + HTML/CSS，除非用户明确要求，不引入 Node、React、数据库、CMS 或复杂构建链。
- 页面设计优先服务长文阅读：正文宽度、字号、行高、移动端体验优先于装饰。
- 不随意修改站名、URL 结构和已发布文章 permalink。

## Git 规则

- 一轮完整修改尽量合并成一次有意义的提交。
- 新增每日文章时，提交信息建议：`publish: 第NNN回 <标题>`。
- 修改站点功能时，提交信息建议：`site: <说明>`。
