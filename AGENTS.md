# AGENTS.md

## 项目定位

本仓库只服务《正在发生的世界史》这一长期连载项目。核心目标是把现实世界每天真正重要的变化，写成一部持续生长、可以长期阅读的中文长篇叙事作品。

## 内容规则

1. 默认每天发布一章，一章对应一个 `docs/_posts/YYYY-MM-DD-NNN.md` 文件。
2. 章节统一使用“第 N 章”的表达；不再使用“回目、评书、定场诗、开讲”等固定章回体包装。
3. 文风按长篇小说处理：允许多线叙事、场景切换、人物视角和文学化转场，但不要为了追求戏剧性虚构现实人物的言行、动机或未发生的事件。
4. 国家、人物、机构、日期、数字、政策、战争、市场与事件关系等事实信息必须有现实依据；不确定的信息不要补写成确定事实。
5. 正文以普通读者能连续读下去为第一优先级，避免新闻简报式堆点，也避免学术论文式表达。
6. 已发布文章原则上不做无痕重写。事实需要纠正时，在文章末尾增加“更正记录”，写明修改日期和内容。
7. 不把评论区传闻、未经确认的社交媒体消息写成既成事实。

## 文件约定

每篇文章 Front Matter 至少包含：

```yaml
---
layout: post
title: "章节标题"
chapter: 1
date: 2026-09-15 21:00:00 +0800
summary: "一句话概括本章"
permalink: /chapters/001/
---
```

正文直接进入叙事，不要求固定开场格式。需要分隔场景时优先使用 Markdown 标题、空行或水平线。

## 站点规则

- GitHub Pages 来源固定为 `main` 分支 `/docs` 目录。
- 站点保持纯 Jekyll + HTML/CSS/原生 JavaScript，除非用户明确要求，不引入 Node、React、数据库、CMS 或复杂构建链。
- 首页只承担作品介绍和阅读入口，不放章节目录。
- `/chapters/` 提供完整章节列表。
- 章节页按阅读器设计，阅读设置使用浏览器 `localStorage` 保存；不得要求登录或服务端存储。
- 阅读体验优先：正文宽度、字号、行高、主题模式、移动端适配优先于装饰。
- 不随意修改站名和已经发布的章节内容；URL 统一使用 `/chapters/NNN/`。

## Git 规则

- 一轮完整修改尽量合并成一次有意义的提交。
- 新增每日文章时，提交信息建议：`publish: 第NNN章 <标题>`。
- 修改站点功能时，提交信息建议：`site: <说明>`。
